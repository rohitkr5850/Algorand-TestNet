version: "3.9"
services:
  frontend:
    build:
      context: ./frontend
    command: npm run dev
    ports:
      - "5173:5173"
    environment:
      - VITE_BACKEND_URL=http://host.docker.internal:5000/api
    volumes:
      - ./frontend:/app
    working_dir: /app
